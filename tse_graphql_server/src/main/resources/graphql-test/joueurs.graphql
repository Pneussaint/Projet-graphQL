query joueurs ($count: Int, $offset: Int) {
    joueurs (count: $count, offset: $offset) {
        id
        nom
        prenom
        poste
        equipe{
            id
            nom
        }
    }
}